SamplerState samp0 : register(s0);
Texture2D heightMap :  register(t2);

struct VS_INPUT 
{
	float2 texCoord : TEXCOORD1;
};

float4 main(VS_INPUT IN) : S_TARGET_OUTPUT
{
	float4 fragColour = heightMap.Sample(samp0, Input.texCoord);
	
	return fragColour;
}
